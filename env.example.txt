# ===========================================
# E-Cell VIT Bhopal - Environment Variables
# ===========================================
# Rename this file to .env and fill in the values.
# Backend Developer: Provide the actual values for local development.

# Backend API Base URL (required when connecting to your server)
# Example: http://localhost:8000/api
# For production: https://api.ecellvitbhopal.com/api
VITE_API_URL=

# Optional: Gemini API Key (if using AI features from AI Studio migration)
# Only needed if frontend uses Gemini directly; otherwise backend handles AI.
GEMINI_API_KEY=

